mcpServers: {}

gateway:
  # Docker MCP Gateway で有効化するサーバー群。
  # chronos-graph のようなクライアントローカル MCP は含めない。
  enabled_servers:
    - SQLite
    - filesystem
    - github-official
    - sequentialthinking
    - skillport
