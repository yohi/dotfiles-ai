[Unit]
Description=Docker MCP Watchdog (Log Rotation and Gateway Refresh)
After=docker-mcp-gateway.service

[Service]
Type=simple
WorkingDirectory=__REPO_ROOT__
ExecStart=/usr/bin/env bash __REPO_ROOT__/_scripts/mcp-watchdog.sh
Restart=always
RestartSec=10

[Install]
WantedBy=default.target
