FROM node:20-slim
RUN apt-get update && apt-get install -y --no-install-recommends gosu && rm -rf /var/lib/apt/lists/*
RUN npm install -g @yohi/nexus@latest typescript
RUN groupadd -r -g 1000 nexus && useradd -r -u 1000 -g 1000 -m nexus
USER nexus
ENTRYPOINT ["nexus"]
