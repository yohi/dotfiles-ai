name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  lint:
    runs-on: ubuntu-slim
    steps:
    - uses: actions/checkout@v4
    - name: Install uv and Python
      uses: astral-sh/setup-uv@v5
      with:
        enable-cache: true
    - name: Set up Python via uv
      run: uv python install 3.10
    - name: Run Linters
      run: uv run --python 3.10 make lint
