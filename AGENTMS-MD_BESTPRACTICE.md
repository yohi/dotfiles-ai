# AI Agent Configuration (AGENTS.md) Best Practices

AIエージェント（Gemini CLI, Claude Code, Cursor等）の挙動を最適化し、開発効率を最大化するための `AGENTS.md` の構成指針です。

## 1. 漸進的開示 (Progressive Disclosure)
すべての指示を1つのファイルに詰め込まず、情報の密度を管理します。

- **Instruction Budget**: エージェントが一度に一貫してフォローできる指示は有限です（150〜200程度）。
- **Root AGENTS.md**: 「プロジェクト概要」「必須ツール」「主要コマンド」「他ドキュメントへのリンク」に絞り、トークン予算を節約します。
- **Hierarchical Docs**: 詳細な規約は `docs/ARCHITECTURE.md` や `docs/SECURITY.md` などに切り出し、エージェントが必要な時に参照させます。

## 2. 優先順位の明示 (Prioritization)
指示に重要度を付与し、エージェントがクリティカルな制約を無視しないようにします。

- **⚠️ MUST (Critical)**: 環境制約（DevContainer必須）、検証ルール（エビデンス必須）、絶対的なセキュリティ要件。
- **🛠️ SHOULD (Standard)**: 標準的なパッケージマネージャーの操作、推奨されるアーキテクチャパターン。
- **💡 MAY (Preferred)**: ドメイン用語の推奨、コードスタイルなどの補助的なガイドライン。

## 3. アイデンティティと境界 (Identity & Boundaries)
エージェントに役割を与え、同時に「やってはいけないこと」を明確にします。

- **👤 Identity**: エージェントの役割、トーン（プロフェッショナル、慎重など）、行動原理を定義します。
- **🛑 Boundaries (DO NOT)**: セキュリティシールドのバイパス、ロックファイルの直接編集、機密情報のログ出力など、明確な禁止事項を定義します。

## 4. 脱出口とエスカレーション (Escalation Path)
絶対的なルールがタスクの障害にならないよう、柔軟性を持たせます。

- **エスカレーションパス**: 「原則として禁止。ただし、例外が必要な場合はエスカレーションせよ」という脱出口（Escape Hatch）を設けます。
- **形式的な条件文**: 「ユーザーが言ったら」といった曖昧な表現ではなく、「Only if a specific User Directive instructs X（特定のユーザー指示がある場合のみXせよ）」のように、形式的な条件を記述します。

## 5. 曖昧な表現の排除 (Vague Qualifiers)
静的解析ツールやLLMの解釈のブレを防ぐため、主観的な単語を排除します。

- **具体的トリガー**: "regularly"（定期的に）、"appropriately"（適切に）、"if necessary"（必要なら）といった単語を避け、「いつ」実行すべきかを明確にします（例："Before reporting task completion", "On every turn"）。
- **定量的基準**: 可能であれば数値や明確なイベント（例："> 2000 chars"）を基準にします。

## 6. 実行可能なツール定義 (Tools & APIs)
エージェントが利用可能なツールとその使用条件を明文化します。

- **ツールマップ**: 各ツールの役割を個別に定義します。
- **実行条件**: 特定のフラグの必須使用（例：`correlation_id` の付与）や、環境の前提条件を記載します。

## 7. 指示の粒度 (Granularity)
LLMの遵守率を最大化するため、指示は最小単位に分解します。

- **One Action per Bullet**: 箇条書き1つにつき、1つのアクションまたは1つの事実のみを記載します。
- **コマンドの分離**: 実行コマンドを単一の項目として独立させ、解釈の余地をなくします。

## 8. 検証による完了定義 (Evidence-based)
エージェントが「できたつもり」になるのを防ぎます。

- **適切な検証手段**: 変更内容に応じ、テストだけでなくCI結果、構成バリデーションなどを要求します。
- **エビデンスの保持**: 証拠（ログ、テスト結果）を保持・提示することを完了の条件とします。

## 9. 鮮度の管理 (Maintenance)
指示の有効性を保つための運用ルールです。

- **Last Updated**: ファイル冒頭に更新日を記載し、指示の鮮度をエージェントが判断できるようにします。

## 10. 静的解析ツールへの対応 (Handling Analysis Tools)
`AGENTS.md` はコードと同様にリポジトリに含まれますが、その特殊な役割ゆえに一般的な静的解析（markdownlint, Codacy等）で偽陽性（False Positive）が発生しやすくなります。

- **解析の除外**: 文書ファイル特有の警告（略語の未定義、曖昧な修飾語、複合指示など）がエージェントへの指示の明瞭さを損なう場合、`.codacy.yml` 等で該当ファイルを解析対象から除外することを検討します。
- **ノイズの抑制**: 解析を継続する場合は、解析ツールの誤検知（例：「DO NOT」の「DO」を略語とみなす等）を許容するか、ルールを個別に無効化して指示の質を優先します。

---

### 推奨されるファイル構造案

```
.
├── AGENTS.md          # ルート：アイデンティティ、必須情報、境界、ツール定義
├── docs/
│   ├── ARCHITECTURE.md # アーキテクチャ・設計規約
│   ├── SECURITY.md     # セキュリティ・監査ログ規約
│   └── TESTING.md      # テスト方針・検証のエビデンス基準
└── AGENTMS-MD_BESTPRACTICE.md # このガイドライン
```
