// vim: ft=jsonc
{
  // Config Modernized: 2026-05-04
  // Target OpenCode Version: v1.14.33
  // Based on oh-my-openagent @dev
  "$schema": "https://opencode.ai/config.json",

  "default_agent": "sisyphus",

  // --------------------------------------------------------------------------
  // 1. コアモデル設定: 最新のフラグシップモデルを配置
  //    タスクの難易度に応じて適切なモデルを使い分けるための基本設定です。
  // --------------------------------------------------------------------------

  // [Plugin]
  "plugin": [
    "@yohi/justice@2.2.0",
    "oh-my-openagent@3.17.12"
  ],

  // --------------------------------------------------------------------------
  // 2. 権限とセキュリティ: 「自律」と「安全」の黄金律
  //    エージェントがスムーズに作業できる「自律性」と、破壊的操作を防ぐ「安全性」のバランスを定義します。
  // --------------------------------------------------------------------------
  "permission": {
    "*": "ask", // 明示的に許可されていない操作は、安全のため常に人間に確認する
    "read": "allow", // ファイルの読み取りは自由（解析に必須）
    "list": "allow", // ディレクトリ一覧の取得（構造理解に必須）
    "glob": "allow", // パターンマッチによるファイル探索
    "grep": "allow", // コード内キーワード検索
    "edit": "allow", // ファイル編集（エージェントの主目的）
    "lsp": "allow", // 言語サーバーによるコード診断情報の取得
    "skill": "allow", // 登録されたスキルの実行
    "websearch": "allow", // 最新情報の収集（ブラウザ検索）
    "bash": {
      "*": "ask", // 任意のコマンド実行は原則として確認が必要
      "ls": "allow",
      "ls *": "allow",
      "cat": "allow",
      "cat *": "allow",
      "head": "allow",
      "head *": "allow",
      "tail": "allow",
      "tail *": "allow",
      "mkdir": "allow",
      "mkdir *": "allow",
      "touch": "allow",
      "touch *": "allow",
      "cp": "allow",
      "cp *": "allow",
      "mv": "allow",
      "mv *": "allow",
      "make": "allow",
      "make *": "allow",
      "uv": "allow",
      "uv *": "allow",
      "uvx": "ask",
      "uvx *": "ask",
      "python": "ask",
      "python *": "ask",
      "node": "ask",
      "node *": "ask",
      "bun": "ask",
      "bun *": "ask",
      "git status": "allow", // リポジトリの状態確認
      "git status *": "allow",
      "git log": "allow", // コミット履歴の確認
      "git log *": "allow",
      "git diff": "allow",
      "git diff *": "allow",
      "git branch": "allow",
      "git branch *": "allow",
      "git checkout": "allow",
      "git checkout *": "allow",
      "git add": "allow",
      "git add *": "deny",
      "git commit": "allow",
      "git commit *": "allow",

      // --- 禁止設定 (Deny): 最優先で評価されるガードレール ---
      "git add .": "deny", // 意図しない全ファイル追加を防止。個別にファイル指定（git add file）を推奨。
      "rm -rf *": "deny", // 破壊的な削除は絶対に許可しない
      "npm publish*": "deny", // 公開操作は必ず人間が行う
      "sudo *": "deny", // 特権操作はエージェントには許可しない
      "su *": "deny", // ユーザー切り替え禁止
      "curl*|sh": "deny", // 外部スクリプトの直接実行は高リスク
      "curl* | sh": "deny",
      "curl *|sh": "deny",
      "curl * | sh": "deny",

      "bash _scripts/*": "ask",
      "bun run lint*": "allow", // 静的解析による品質確認
      "bun run typecheck*": "allow", // 型チェックによる検証
      "bun run test*": "allow", // テスト実行による動作検証
      "bun run build*": "allow", // ビルド確認（構文エラーの最終確認）
      "dmypy*": "allow", // mypyデーモンによる高速な型チェック
      "shellcheck*": "allow", // シェルスクリプトの文法確認
      "eslint*": "allow", // JS/TSのスタイル・バグ確認

      // --- 確認設定 (Ask): 外部影響やコンテキストの大きな変化を伴う操作 ---
      "git push*": "ask", // リモートへの反映は最終的な人間の承認が必要
      "git reset*": "ask", // 作業内容の巻き戻しは意図を確認する
      "git clean*": "ask", // 未追跡ファイルの削除
      "git pull*": "ask", // コンフリクトのリスクがあるため確認
      "gh pr create*": "ask" // プルリクエスト作成は外部向けの意思表示
    }
  },

  // --------------------------------------------------------------------------
  // 3. コンテキスト・ライフサイクル管理
  //    エージェントの「記憶」の質と量を最適化し、長時間の対話でも精度を維持します。
  // --------------------------------------------------------------------------
  "compaction": {
    "auto": true, // 会話が長くなった際に自動で重要事項を要約する
    "reserved": 12000, // 要約バッファを広めに確保（複雑なタスクでもコンテキスト落ちしにくくする）
    "tail_turns": 3, // 直近の数ターンは原文をそのまま保持し、現在の文脈を正確に把握させる
    "prune": true // 不要なツールの実行結果などを積極的に削除してトークンを節約する
  },

  // --------------------------------------------------------------------------
  // 4. 言語サーバー (LSP) / 静的解析ツール: コード理解の精度向上
  //    AIがコードを書くだけでなく、その正しさをリアルタイムに自己診断するための設定です。
  // --------------------------------------------------------------------------
  "lsp": {
    "typescript-language-server": {
      "command": [
        "typescript-language-server",
        "--stdio"
      ],
      "extensions": [
        ".ts",
        ".tsx",
        ".js",
        ".jsx"
      ],
      "initialization": {
        "preferences": {
          "includeInlayParameterNameHints": "all",
          "includeInlayVariableTypeHints": true,
          "includeInlayFunctionParameterTypeHints": true,
          "disableSuggestions": false
        },
        "diagnostics": {
          "ignoredCodes": [
            6133
          ]
        }
      }
    },
    "bash-language-server": {
      "command": [
        "bash-language-server",
        "start"
      ],
      "extensions": [
        ".sh"
      ]
    },
    "pyright": {
      "command": [
        "pyright-langserver",
        "--stdio"
      ],
      "extensions": [
        ".py"
      ],
      "initialization": {
        "python": {
          "analysis": {
            "typeCheckingMode": "strict",
            "autoSearchPaths": true,
            "useLibraryCodeForTypes": true
          }
        }
      }
    },
    "markdownlint": {
      "command": [
        "npx",
        "markdownlint-lsp",
        "--stdio"
      ],
      "extensions": [
        ".md"
      ]
    },
    "mypy": {
      "command": [
        "mypy",
        "--ignore-missing-imports",
        "--follow-imports=silent",
        "--strict"
      ],
      "extensions": [
        ".py"
      ]
    },
    "shellcheck": {
      "command": [
        "shellcheck",
        "-f",
        "gcc", // OpenCodeがエラー箇所を正確に把握しやすいgcc形式を指定
        "--severity=style"
      ],
      "extensions": [
        ".sh"
      ]
    },
    "eslint": {
      "command": [
        "eslint",
        "--format",
        "compact", // 出力を簡潔にし、コンテキストの圧迫を防ぎつつ正確な情報を伝達
        "--max-warnings",
        "0"
      ],
      "extensions": [
        ".ts",
        ".tsx",
        ".js",
        ".jsx"
      ]
    }
  },

  // --------------------------------------------------------------------------
  // 5. 実験的・最新機能の有効化
  //    OpenCodeのポテンシャルを最大限に引き出すための最適化設定です。
  // --------------------------------------------------------------------------
  "experimental": {
    "batch_tool": true, // 複数ツール実行をバッチ化して通信回数を減らし、高速化する
    "continue_loop_on_deny": false, // コマンド実行が拒否された場合は思考を停止させ、安全を確保する
    "mcp_timeout": 10000 // MCP (Model Context Protocol) リクエストのタイムアウトを延長
  },

  // --------------------------------------------------------------------------
  // 6. 外部リソース・ルール
  //    エージェントが準拠すべきコーディング標準や共通ルールを定義します。
  // --------------------------------------------------------------------------
  "instructions": [
    "global-rules/AGENTS.global.md", // 全プロジェクト共通の行動指針
    "agent-skills/superpowers/using-superpowers/SKILL.md"
  ],
  "watcher": {
    "ignore": [
      "**/node_modules/**",
      "**/dist/**",
      "**/coverage/**",
      "**/*.lock",
      "**/.git/**"
    ]
  },

  // --------------------------------------------------------------------------
  // 7. プロバイダー構成 (主要なA2AおよびAPI接続)
  //    利用可能なモデルのホワイトリストを管理し、適切な推論コストと精度を選択します。
  //    モデル識別子は最新のスキーマ (models.dev) に基づき "provider/name" 形式で記述します。
  // --------------------------------------------------------------------------
  "enabled_providers": [
    "cloudflare-workers-ai",
    "openai",
    "google",
    "nvidia",
    "opencode"
  ],
  "provider": {
    "cloudflare-workers-ai": {
      "whitelist": [
        "@cf/moonshotai/kimi-k2.6",
        "@cf/zai-org/glm-4.7-flash",
        "@cf/meta/llama-4-scout-17b-16e-instruct"
      ]
    },
    "openai": {
      "whitelist": [
        "gpt-5.5",
        "gpt-5.4",
        "gpt-5.3-codex"
      ]
    },
    "google": {
      "whitelist": [
        "gemini-3-flash-preview",
        "gemini-3.1-flash-image-preview",
        "gemini-3.1-pro-preview"
      ]
    },
    "nvidia": {
      "whitelist": [
        "moonshotai/kimi-k2.6",
        "z-ai/glm-5.1",
        "qwen/qwen3.5-397b-a17b"
      ]
    },
    "opencode": {
      "whitelist": [
        "minimax-m2.5-free",
        "big-pickle"
      ]
    }
  },
  // [MCP]
  "mcp": {
    "docker-mcp": {
      "url": "http://127.0.0.1:10888/sse",
      "headers": {
        "Authorization": "Bearer {env:MCP_GATEWAY_TOKEN}"
      },
      "type": "remote",
      "enabled": true
    }
  }
}
