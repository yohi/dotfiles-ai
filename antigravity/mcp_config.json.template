{
  "mcpServers": {
    "docker-mcp-gateway": {
      "command": "docker",
      "args": [
        "mcp",
        "gateway",
        "run",
        "--catalog",
        "__HOME__/.docker/mcp/catalogs/bootstrap.yaml",
        "--catalog",
        "__HOME__/.docker/mcp/catalogs/custom.yaml"
      ]
    }
  }
}
