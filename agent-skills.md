# Agent Skills 運用ベストプラクティス: APM & Skillport ハイブリッド構成

本文書では、**APM (Agent Package Manager)** による静的管理と、**Skillport** による動的実行を組み合わせた、2026年時点での AI エージェント運用のベストプラクティスを定義します。

---

## 1. コンセプト：役割の分離
エージェントの能力（スキル）を最大限に引き出すため、以下の2つのレイヤーを明確に分離します。

| レイヤー | ツール | 役割 | 相当する概念 |
| :--- | :--- | :--- | :--- |
| **管理層 (Static)** | **APM** | スキルの配備・バージョニング・設定同期 | `npm` / `pip` |
| **実行層 (Dynamic)** | **Skillport** | 対話中のスキル検索・ロード（MCPサーバー） | `Runtime` / `API` |

**「APMで環境を整え、Skillportという機能を使ってエージェントが自律的に動く」** 構造が基本となります。

---

## 2. 4つのベストプラクティス

### Layer 1: APM を「唯一の正解 (SSOT)」にする
すべてのエージェント設定は `apm.yml` で一括管理します。

- **集中管理**: スキルの依存関係（Git、Marketplace）および MCP サーバー（Skillport, Nexus等）の定義を `apm.yml` に集約。
- **再現性**: `apm.lock.yaml` をコミットし、チーム全員が同一バージョンのスキルを使用することを保証。
- **自動同期**: 手動での設定変更を禁止し、常に `make sync-mcp` (内部で `apm install`) を通じて全エージェントの設定を同期。

### Layer 2: Skillport を「スキルの窓口」として常駐させる
エージェントが対話中に必要な知識を自ら取得できる環境を提供します。

- **動的ロード**: エージェントの初期プロンプトに全スキルを詰め込むのではなく、Skillport MCP 経由で `search_skills` / `load_skill` を行わせる。
- **コンテキスト節約**: 必要な時だけ特定の `SKILL.md` を読み込ませることで、トークン使用量を抑え、推論精度を維持。

### Layer 3: 標準的なプロジェクト構成
APM と Skillport がシームレスに連携するディレクトリ構成を維持します。

```text
.
├── apm.yml                # マスター設定（依存関係、MCP定義）
├── apm.lock.yaml          # バージョンロック
├── Makefile               # 'make sync-mcp' 等の自動化コマンド
├── agent-skills/          # スキルの実体 (Skillport の参照先)
│   ├── .skillport/        # Skillport メタデータ
│   └── my-skill/
│       └── SKILL.md       # スキルの定義ファイル
└── global-rules/          # 全エージェント共通の指示 (AGENTS.global.md)
```

### Layer 4: 運用ワークフロー
人間とエージェントの役割分担を明確にします。

1. **スキルの追加 (人間)**: `apm.yml` にリポジトリ名を追加し、`make sync-mcp` を実行。
2. **スキルの発見 (エージェント)**: タスク中、エージェントは自律的に Skillport で関連スキルを検索。
3. **スキルの実行 (エージェント)**: `load_skill` で取得した指示に基づき、タスクを完遂。
4. **ガバナンス (CI)**: `apm audit --ci` を回し、未承認のスキルやセキュリティリスクのある MCP 接続を遮断。

---

## 3. 導入のメリット

- **ポータビリティ**: `git clone` して `make setup` するだけで、複雑なマルチエージェント環境が即座に立ち上がる。
- **マルチクライアント対応**: Claude Code, Cursor, Gemini CLI, OpenCode 等、ツールを問わず同じスキルと設定を共有できる。
- **安全性**: Microsoft APM のポリシー機能により、組織レベルでの安全なエージェント運用が可能。

---
*最終更新: 2026年5月16日*
