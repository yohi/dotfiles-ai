# Skillport MCP 調査および設定不備解消報告書

## 1. 目的
Gemini CLI の MCP モードにおいて、プロジェクト固有のスキル（`agent-skills/` 配下）が `activate_skill` ツールや Skillport 経由で認識されない問題を調査し、その設定不備を解消する。

## 2. 特定された不備と原因
調査の結果、以下の 4 点の不備が連鎖して発生していたことが判明しました。

### ① スキルディレクトリの認識不一致（標準 vs 独自）
*   **不備**: Gemini CLI 組み込みの `activate_skill` ツールは `.gemini/skills/` 等の標準パスを期待するが、本プロジェクトは `agent-skills/` という独自ディレクトリを使用していた。
*   **原因**: プロジェクトの独自構造が Gemini CLI の標準的な自動検出対象から外れていた。

### ② MCP カタログ（Volume/環境変数）の欠落
*   **不備**: `skillport` MCP サーバーの設定（`custom.yaml`）において、プロジェクトディレクトリが Docker コンテナにマウントされていなかった。
*   **原因**: グローバルな Skillport 設定が `__HOME__/.skillport/skills` のみを参照しており、プロジェクト内の `agent-skills/` をスキャン対象に含めていなかった。

### ③ Docker イメージのビルド失敗
*   **不備**: `skillport` サーバーが使用する Docker イメージ (`ghcr.io/yohi/skillport:latest`) が環境に存在せず、ビルドも失敗していた。
*   **原因**: `Dockerfile.skillport` が期待する `entrypoint-skillport.sh` のパスが、実際の配置（`mcp/`）とビルドコンテキストの間で食い違っていた。

### ④ 同期スクリプトによる設定の上書き
*   **不備**: `make sync-mcp` を実行するたびに、修正した設定がテンプレート（`.template`）によって初期化（`__REPO_ROOT__` の未置換状態など）されていた。
*   **原因**: テンプレートファイル側を修正せずに、レンダリング後のファイルを直接修正していたため、同期のタイミングで修正が消失していた。

## 3. 実施した修正内容
以下の手順で「正のデータ（SSOT）」を修正し、反映を行いました。

1.  **テンプレートの修正**: `mcp/catalogs/custom.yaml.template` を修正し、プロジェクトディレクトリのマウント設定と、`SKILLPORT_SKILLS_DIR` へのパス追加を恒久化。
2.  **config.yaml の修正**: ゲートウェイの負荷を抑え、Skillport を確実に有効化するため、`mcp/config.yaml` を最小構成に整理。
3.  **Docker イメージの再構築**: 正しいビルドコンテキスト（`mcp/`）を指定して `skillport:latest` イメージをローカルでビルド。
4.  **シンボリックリンクの解消**: 混乱を避けるため、Gemini CLI の標準パスへの一時的なリンクを削除し、本来の Skillport MCP 経由のパス（`agent-skills/`）を確立。

## 4. 現在の状態と残された課題
### 現在の状態
*   **設定レベルの修正は完了**: `custom.yaml` および `config.yaml` は、プロジェクトのスキルを認識するための正しい設定になっています。
*   **イメージの準備完了**: Skillport サーバーを稼働させるための Dockerイメージはローカルに存在し、実行可能です。

### 残された課題（要観察）
*   **ゲートウェイの不安定性**: `docker-mcp-gateway` が起動プロセスにおいて `too many open files` エラーを吐き、クラッシュループに陥る現象が確認されました。
*   **ツールの露出**: ゲートウェイが不安定なため、エージェントのツールリストに `search_skills` や `load_skill` が現れない（または反映に時間がかかる）場合があります。

## 5. 今後の推奨アクション
1.  **ゲートウェイの安定化**: クラッシュループが続く場合は、ホストOSを再起動するか、`docker system prune` 等でリソースをクリーンアップすることを推奨します。
2.  **フォールバック運用**: MCP 経由のツール呼び出しが不安定な間は、エージェントに対し「`agent-skills/` 内のスキルファイルを直接 `read_file` で読み込んで指示に従う」よう命じることで、本来のワークフロー（using-superpowers 等）を遂行可能です。
